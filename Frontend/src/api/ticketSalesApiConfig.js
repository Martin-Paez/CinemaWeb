const HOST = "localhost";
const PORT = "7067";

const PROTOCOL = "https";
const API_VERSION = "/api/v1";

const TICKET_SALES_API_URL = `${PROTOCOL}://${HOST}:${PORT}${API_VERSION}`;

export default TICKET_SALES_API_URL;
